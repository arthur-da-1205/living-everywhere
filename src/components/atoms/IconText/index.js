import React from "react";

import { Button } from "components";

const IconText = () => {
  return (
    <Button className="brand-text-icon" href="" type="link">
      liv<span className="text-gray-800">Everywhere.</span>
    </Button>
  );
};

export default IconText;
